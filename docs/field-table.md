# Field table

Every column of every table the app reads. This file is generated by `scripts/field-table.py`
from the AOSP schema in `third_party/aosp/` and from the policy the app implements, so it cannot
drift from either.

<!-- BEGIN FIELD TABLE -->
Every column of every table the app reads. *Preserved* says what happens to the value on
the destination:

- **yes** — written from the backup.
- **remapped** — not carried verbatim because it identifies a row the destination provider
  owns; the provider assigns the equivalent itself.
- **derived** — not written, but recomputed by the destination provider from data that *is*
  copied, so the information survives even though the column does not.
- **no** — not reproduced at all; the reason column says why.

*Supported by ICS* is there for comparison with an ICS-based transfer: **yes** means an
RFC 5545 property carries the value, **partial** means it is carried with a caveat, and
**no** means iCalendar has no equivalent at all.

#### `Calendars`

The destination calendar already exists, so nothing in this table is written. Names and colours are still captured in the backup: they label the import preview, and they are what the Calendars columns on each event row refer to.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `_id` | Row id | **remapped** | local row identity; the destination provider assigns its own and the backup id is used only to remap relationships | **no** |
| `account_name` | account the row was synced from | **no** | identifies the source account; the destination calendar belongs to a different account | **no** |
| `account_type` | type of that account | **no** | identifies the source account; the destination calendar belongs to a different account | **no** |
| `_sync_id` | the id the sync source assigned to this row | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `dirty` | whether the row has unsynced local changes | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `mutators` | which packages wrote those local changes | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `deleted` | whether the row is a pending deletion | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `canPartiallyUpdate` | whether edits keep a pre-edit copy for the adapter | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync1` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync2` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync3` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync4` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync5` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync6` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync7` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync8` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync9` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `cal_sync10` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `calendar_color` | the calendar's colour | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | partial — RFC 7986 `COLOR`, a CSS3 name rather than an ARGB integer |
| `calendar_color_index` | key into the Colors table for the calendar colour | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `calendar_displayName` | the calendar's display name | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | partial — RFC 7986 `NAME`, or the de-facto `X-WR-CALNAME`; most exporters ignore both |
| `calendar_access_level` | the user's access level for the calendar | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `visible` | whether the calendar is shown | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `calendar_timezone` | the calendar's default time zone | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | partial — a `VTIMEZONE` block or the de-facto `X-WR-TIMEZONE` |
| `sync_events` | whether the calendar is synced | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `ownerAccount` | the account that owns the calendar | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `canOrganizerRespond` | whether the user may reply as organiser | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `canModifyTimeZone` | whether the user may change the calendar time zone | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `maxReminders` | how many reminders the calendar allows | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `allowedReminders` | reminder methods the calendar allows | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `allowedAvailability` | availability values the calendar allows | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `allowedAttendeeTypes` | guest types the calendar allows | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `isPrimary` | whether this is the account's primary calendar | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |
| `name` | calendar name used by the sync adapter | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | partial — the de-facto `X-WR-CALNAME` |
| `calendar_location` | geographic location of the calendar | **no** | the destination calendar already exists, so its name, colour, visibility and limits are left untouched | **no** |

#### `Events`

One row per event or recurring series. The provider surfaces the sync columns on this row too and joins in the calendar attributes listed above, which is why writing a dumped row straight back fails.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `calendar_id` | which calendar the event belongs to | **remapped** | the destination calendar id is written instead | **no** — an iCalendar object has no addressable calendar; grouping is per file |
| `title` | the event title | **yes** | — | yes — `SUMMARY` |
| `description` | the event notes | **yes** | — | yes — `DESCRIPTION` |
| `eventLocation` | Where the event takes place | **yes** | — | yes — `LOCATION` |
| `eventColor` | a per-event colour that overrides the calendar colour | **yes** | — | partial — RFC 7986 `COLOR` is a calendar-level CSS3 name, not a per-event ARGB integer |
| `eventColor_index` | key into the Colors table for a per-event colour | **no** | a colour **key**; an unresolvable key either fails the insert outright or silently nulls the colour, so only the literal colour is written | **no** |
| `displayColor` | the colour an app should draw: the event colour, falling back to the calendar colour | **derived** | computed as the event colour, falling back to the destination calendar's colour | **no** |
| `eventStatus` | Event status | **yes** | — | yes — `STATUS` |
| `selfAttendeeStatus` | copy of the owner's attendee status | **derived** | the provider recomputes it from the attendee rows, but only from the attendee whose address equals the destination calendar's owner account, so it can end up 'none' when the source and target accounts differ. The reply itself is carried by that attendee row | yes — `PARTSTAT` on your own `ATTENDEE` |
| `sync_data1` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data2` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data3` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data4` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data5` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data6` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data7` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data8` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data9` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `sync_data10` | sync adapter scratch space | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `lastSynced` | pre-edit copy marker | **no** | recomputed by the destination provider from the data that is copied | **no** |
| `dtstart` | Time the event starts in UTC millis since epoch | **yes** | — | yes — `DTSTART` (with `TZID`) |
| `dtend` | Time the event ends in UTC millis since epoch | **yes** | — | yes — `DTEND` |
| `duration` | Duration of the event in RFC2445 format | **yes** | — | yes — `DURATION` |
| `eventTimezone` | the event's time zone, stored separately from the UTC start instant | **yes** | — | yes — the `TZID` parameter on `DTSTART` |
| `eventEndTimezone` | Timezone for the end time of the event | **yes** | — | yes — the `TZID` parameter on `DTEND` |
| `allDay` | Is the event all day (time zone independent) | **yes** | — | yes — `DTSTART;VALUE=DATE` |
| `accessLevel` | Defines how the event shows up for others when the calendar is shared | **yes** | — | partial — `CLASS` has PUBLIC / PRIVATE / CONFIDENTIAL, so the provider's "default" has no form |
| `availability` | Default access is controlled by the server and will be treated as public on the device | **yes** | — | partial — `TRANSP` is only OPAQUE / TRANSPARENT, so "tentative" has no form |
| `hasAlarm` | whether the event has a reminder | **derived** | recomputed from the reminders that are copied | **no** |
| `hasExtendedProperties` | whether the event has extended properties | **derived** | recomputed from ExtendedProperties, which is not copied, so it reads 0 on the destination | **no** |
| `rrule` | Recurrence rule for the event | **yes** | — | yes — `RRULE` |
| `rdate` | Recurrence dates for the event | **yes** | — | yes — `RDATE` |
| `exrule` | Recurrence exception rule for the event | **yes** | — | **no** — `EXRULE` existed in RFC 2445 but was removed in RFC 5545 |
| `exdate` | Recurrence exception dates for the event | **yes** | — | yes — `EXDATE` |
| `original_id` | the source event this override replaces | **remapped** | set to the new row id of the series when the provider creates the override | **no** |
| `original_sync_id` | the source series' sync id | **no** | the source account's identity / sync namespace, which does not exist for the destination user | **no** |
| `originalInstanceTime` | Original instance time of the recurring event for which this event is an exception | **yes** | — | yes — `RECURRENCE-ID` |
| `originalAllDay` | all-day flag of the source series | **derived** | inherited from the series when the provider creates the override | **no** |
| `lastDate` | the last date the recurrence repeats on | **derived** | recomputed from the recurrence rule | **no** |
| `hasAttendeeData` | whether full guest data is present | **derived** | recomputed from the attendee rows that are copied | **no** |
| `guestsCanModify` | Whether guests can modify the event | **yes** | — | **no** |
| `guestsCanInviteOthers` | Whether guests can invite other guests | **yes** | — | **no** |
| `guestsCanSeeGuests` | Whether guests can see the list of attendees | **yes** | — | **no** |
| `organizer` | Email of the organizer (owner) of the event | **yes** | — | yes — `ORGANIZER` |
| `isOrganizer` | whether the user organises this event | **derived** | recomputed by comparing organizer with the destination calendar's owner account | **no** |
| `canInviteOthers` | whether the user may invite others | **derived** | computed by the provider from the guest permissions and the access level | **no** |
| `customAppPackage` | Package name of the custom app that can provide a richer experience for the event | **no** | a pointer to the app that owns this event's UI, not event content. The calendar app hands such an event to that package through ACTION_HANDLE_CUSTOM_EVENT, passing the paired URI as an opaque extra: the platform defines no form for it, never resolves or validates it, and only that app can interpret it. So nothing on the destination can tell a still-valid pointer from a stale one, the package need not exist there, and a stale pointer would fail silently | **no** |
| `customAppUri` | URI used by the custom app for the event | **no** | a pointer to the app that owns this event's UI, not event content. The calendar app hands such an event to that package through ACTION_HANDLE_CUSTOM_EVENT, passing the paired URI as an opaque extra: the platform defines no form for it, never resolves or validates it, and only that app can interpret it. So nothing on the destination can tell a still-valid pointer from a stale one, the package need not exist there, and a stale pointer would fail silently | **no** |
| `uid2445` | iCalendar UID for events that arrived from an .ics file | **yes** | — | yes — `UID` |
| `account_name` | account the row was synced from | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `account_type` | type of that account | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `_sync_id` | the id the sync source assigned to this row | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `dirty` | whether the row has unsynced local changes | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `mutators` | which packages wrote those local changes | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `deleted` | whether the row is a pending deletion | **no** | sync bookkeeping owned by the source account's adapter: meaningless in another account, and the destination provider rewrites it anyway | **no** |
| `canPartiallyUpdate` | whether edits keep a pre-edit copy for the adapter | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync1` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync2` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync3` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync4` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync5` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync6` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync7` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync8` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync9` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |
| `cal_sync10` | sync adapter scratch space | **no** | a calendar attribute surfaced on the event row; the destination calendar already exists and keeps its own | **no** |

#### `Attendees`

One row per guest per event.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `_id` | Row id | **remapped** | local row identity; the destination provider assigns its own and the backup id is used only to remap relationships | **no** |
| `event_id` | Id of the event | **remapped** | rewritten to the new event id the destination provider assigned | **no** |
| `attendeeName` | Name of the attendee | **yes** | — | yes — `ATTENDEE;CN=` |
| `attendeeEmail` | Email address of the attendee | **yes** | — | yes — the `ATTENDEE` value |
| `attendeeRelationship` | guest / organiser | **yes** | — | partial — `ROLE`, but performer and speaker have no iCalendar equivalent |
| `attendeeType` | required / optional / resource | **yes** | — | partial — `CUTYPE`, but the provider stores no room/individual distinction |
| `attendeeStatus` | the guest's reply | **yes** | — | yes — `PARTSTAT` |
| `attendeeIdentity` | identity of the guest in the source account | **no** | the source account's identity / sync namespace, which does not exist for the destination user | **no** |
| `attendeeIdNamespace` | namespace that identity belongs to | **no** | the source account's identity / sync namespace, which does not exist for the destination user | **no** |

#### `Reminders`

One row per reminder per event.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `_id` | Row id | **remapped** | local row identity; the destination provider assigns its own and the backup id is used only to remap relationships | **no** |
| `event_id` | Event the reminder belongs to | **remapped** | rewritten to the new event id the destination provider assigned | **no** |
| `minutes` | Minutes prior to the event that the alarm should ring | **yes** | — | partial — `VALARM;TRIGGER`, except the provider's "use the system default" (-1) |
| `method` | the reminder method: alert / email / sms / default | **yes** | — | partial — `VALARM;ACTION`, which has DISPLAY / EMAIL / AUDIO but no SMS |

#### `ExtendedProperties`

Sync adapters' private scratch space.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `_id` | Row id | **remapped** | local row identity; the destination provider assigns its own and the backup id is used only to remap relationships | **no** |
| `event_id` | Event the extended property belongs to | **remapped** | rewritten to the new event id the destination provider assigned | **no** |
| `name` | calendar name used by the sync adapter | **no** | the ExtendedProperties table is writable only by a sync adapter, which this app is not | partial — the de-facto `X-WR-CALNAME` |
| `value` | the property value | **no** | the ExtendedProperties table is writable only by a sync adapter, which this app is not | partial — an exporter could emit these as `X-` properties, but the format has no notion of them |

#### `Colors`

The per-account colour palette that the `*_color_index` columns point into.

| Field | Description | Preserved | Reason when not preserved | Supported by ICS |
|---|---|---|---|---|
| `_id` | Row id | **remapped** | local row identity; the destination provider assigns its own and the backup id is used only to remap relationships | **no** |
| `account_name` | account the row was synced from | **no** | the Colors table is writable only by a sync adapter, which this app is not | **no** |
| `account_type` | type of that account | **no** | the Colors table is writable only by a sync adapter, which this app is not | **no** |
| `data` | opaque sync data | **no** | the Colors table is writable only by a sync adapter, which this app is not | **no** |
| `color_type` | whether the colour is for a calendar or an event | **no** | the Colors table is writable only by a sync adapter, which this app is not | **no** |
| `color_index` | colour key | **no** | the Colors table is writable only by a sync adapter, which this app is not | **no** |
| `color` | 8-bit ARGB colour | **no** | the Colors table is writable only by a sync adapter, which this app is not | partial — RFC 7986 `COLOR` is a CSS3 colour name, not an ARGB integer |

<!-- END FIELD TABLE -->
